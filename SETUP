Global setup:
 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email michaelthecsguy@gmail.com
        
Next steps:
  mkdir dotFiles-Utilities
  cd dotFiles-Utilities
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:michaelthecsguy/dotFiles-Utilities.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:michaelthecsguy/dotFiles-Utilities.git
  git push -u origin master

Setup
  1. git clone
  2. at user root, touch .file and then ln -sf .file from git local repo to
     .file in user's root directory
